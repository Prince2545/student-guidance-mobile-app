# Nvidia / OpenAI-compatible upstream (server-side only — never commit real keys)
NVIDIA_API_KEY=
# Optional:
# API_KEY=
# AI_API_URL=https://integrate.api.nvidia.com/v1/chat/completions
# AI_MODEL=meta/llama-3.1-8b-instruct
